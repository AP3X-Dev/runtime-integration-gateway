# rig-core

Core library for RIG.

Includes:
- RTP (RIG Tool Protocol) data models
- Tool registry and interface hashing
- Runtime execution pipeline (validation, secrets, policy, retries, approvals)
- Minimal audit logger
